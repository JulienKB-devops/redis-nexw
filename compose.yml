services:
  redis:
    build:
      context: ./
      dockerfile: ./Dockerfile
    #image: optimiste/redis:latest
    image: optimiste/redis:1.0.0-debian
    container_name: redis-julien
    restart: unless-stopped
    volumes:
      - redis:/data
      - ./redis.conf:/etc/redis/redis.conf
      #- ./logs:/var/log/redis
    security_opt:
      - no-new-privileges:true

volumes:
  redis:

networks:
  default:
    name: redis-julien
